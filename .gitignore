/.venv
__pycache__/
/.vscode
/.env
*.db
*.db-shm
*.db-wal
# pre-migration snapshots of companion.db
*.db.bak-*

# generated bake-off transcripts (the harness itself lives in scripts/)
/bakeoff/

# v1 archived config holds a hardcoded token; must never be committed
/archive/v1/infrastructure/config.py
/.claude

# local git-history backups (bundles). NEVER publish — these hold the pre-scrub
# history, including the old hardcoded token.
/backups/