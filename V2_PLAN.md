# ai-memory-project — V2.0 Plan

**Status:** planning (no code yet). This document is the deliberate answer to the open questions in the V1 retrospective (`V1_RETROSPECTIVE.md`), grounded in the decisions made during v2 planning.

---

## 0. Locked decisions (from planning)

| Decision | Choice |
|---|---|
| LLM serving | **Fully local** (LM Studio / OpenAI-compatible), continuing v1's deliberate local-first stance |
| Interface | **Python-native web UI: FastAPI + WebSocket**, minimal frontend; voice-in/voice-out slots in later behind the same pipeline |
| First goal | A **solid, trustworthy base**: (1) realistic personality, (2) fast memory recall + saving, (3) a working tick loop |
| Pillar build order | **Memory lifecycle → Emotion persistence+eval → Proactivity → Tool framework** |
| What a tick does | **All four**: internal reflection, mood drift, proactive messages, self-initiated tool use |
| Persona identity | **Emergent, self-modifying prompt.** Simple starting persona ("be friendly, but a stranger"); the bot rewrites a dedicated section of its own prompt over time during idle ticks as it gets to know the user. Low priority vs. the basics. Scaffolding already exists in v1 (`prompt_builder` injects a `Your self described personality` slot). |
| Emotion | **Keep the v1 approach** (RoBERTa GoEmotions → 6 channels + decay). It's lightweight and low-overhead — important on limited hardware. Add persistence; run the classifier on **CPU** (AMD 9070XT can't use the v1 CUDA `device=0` path, and CPU keeps all 16GB VRAM free for LLMs). |
| Model choice | **Decide empirically via an early bake-off** across LM Studio models. Primary axis: **personality vs. speed.** |
| Tech continuity | **Open to rethinking anything** — each subsystem below flags its trade-off explicitly |
| Overriding priority | **Get the skeleton running end-to-end as fast as possible.** Minimal Phase 0, real chat against the real stack ASAP, then layer. |
| V2 finish line | **Bare-bones vertical slice + a rigorous model bake-off first.** Get chat + autonomic memory + emotion + basic tick + logging genuinely good, thoroughly test personality & performance across multiple models, *then* decide how far to scale. Front-load only the cheap seams; defer heavy/uncertain work. |
| Runtime | **Single async process** (FastAPI/asyncio); blocking work (RoBERTa/torch) offloaded to a threadpool; a **priority queue** arbitrates the one-model-at-a-time GPU. Brain reachable only via the `Companion` facade so a future brain-daemon split stays a contained change. |
| Capability model | **Three tiers by mechanism** (see §1.1): autonomic pipeline stages (recall/emotion), deliberate-internal via structured output (lifecycle/self-edit), deliberate-external via true tool-calling (music/web/reminders). Only the last is uncertain on local models — test it before betting on it. |

**Hardware constraint:** AMD Radeon 9070XT (16GB VRAM), not a high-end machine. Everything must fit a modest VRAM/compute budget — favors small models, CPU for the tiny emotion classifier, and on-demand model load/unload (see Sleep mode, §2.8).

**Guiding principle carried from v1 (validated under real use):** the companion is a *brain* (memory + emotion + conversation) with a *delivery layer* (proactivity + tools + voice) on top. The brain must be trustworthy before the delivery layer earns its keep.

---

## 1. Architecture — the "do it right from day one" lessons

These are v1 lessons that cost real rework the first time. They are non-negotiable starting conditions for v2, from the first commit.

- **Strict layering + one composition root.** `infrastructure/` (persistence, LLM client, embedder, emotion model) sits strictly below `core/` (domain managers, facade, chat loop, tick loop), wired in one place. This is what made v1's Postgres→SQLite swap a contained change — keep it.
- **Store vs. Manager separation, with a Store `Protocol` defined day one.** A Store does only persistence (queries in, plain records out); a Manager coordinates a Store with collaborators (embedder, LLM, emotion). The Protocol documents the swap seam *and* enables an in-memory fake for deterministic tests. V1 added this late — v2 starts with it.
- **A deliberate facade / entry-point API from day one.** No reach-through coupling (`loop.companion.manager.sub.method()`). Callers talk to the `Companion` facade only.
- **All prompt text lives in exactly one place.** One prompt module owns every string sent to any model — persona, extraction, reflection, tool-use. No inline prompt strings drifting into managers.
- **Scoped logging from commit one.** Scope DEBUG to our own package(s); pin httpx / huggingface_hub / openai client loggers to WARNING. Never `basicConfig(level=DEBUG)` globally. No `print()` debugging that has to be cleaned up later.
- **Secrets via `.env` / real secrets flow from the start.** No hardcoded tokens in config files (a v1 open issue).

### 1.1 Capability tiers: autonomic vs. deliberate (key scaling decision)

Not everything the companion does is a "tool." Capabilities split into **three tiers by mechanism**, mirroring how a brain works — and this split *de-risks the whole project*, because the core brain lives in the two reliable tiers.

| Tier | Mechanism | Examples | Local-model reliability |
|---|---|---|---|
| **1 — Autonomic (involuntary)** | Pipeline stages that run around every cognition step; the model never "chooses" them | **Recall** (embed context → vector search → inject into system prompt), **emotional reaction**, **memory formation** (backgrounded extraction) | N/A — deterministic code, no model choice |
| **2 — Deliberate-internal** | Model chooses, but expresses it by **filling a JSON schema** (schema-constrained decoding) | **Memory lifecycle** (new/update/supersede), **self-edit** of the persona section | **Reliable** locally (v1 proved schema-constrained output works and even suppresses `<think>`) |
| **3 — Deliberate-external** | Model emits a real **tool/function call** | **Navidrome/music**, **web search**, **reminders** | **Uncertain** — must be tested per model before we lean on it |

Consequences:
- **Recall is not a tool.** It happens automatically every turn, like memory surfacing unbidden. Memory works regardless of whether the chosen model can function-call.
- **Only Tier 3 gates on tool-calling reliability.** Flaky function-calling limits delivery-layer extras, never whether the companion remembers you or stays in character. This is why the bake-off scores tool-calling as a **separate** axis (§2.1 / Phase 0).
- This resolves the earlier "is everything a tool?" question: **no** — autonomic functions are the involuntary substrate, structured-output is the reliable middle, and true tools are reserved for external, deliberate actions.

### 1.2 Runtime & concurrency (decided: single process, brain extractable)

- **Physical constraint:** one GPU ⇒ **one model call at a time**, no matter the architecture. Everything else is about what wraps that serialization point.
- **Decision: single async process** (FastAPI + asyncio). LLM calls are async HTTP to LM Studio (never block the loop); the one blocking dependency (RoBERTa on CPU / torch) runs in a **threadpool executor**. The UI, presence pings, and WebSocket stay responsive during long generations.
- **Model-access arbiter = a priority queue, not a boolean lock.** Contention order: **user turn > proactive reply > background reflection.** This is the scalable replacement for v1's single skip-if-busy lock.
- **Sleep fits naturally:** the process stays alive and lightweight while models are unloaded, so the tick/heartbeat keeps running to decide when to wake. The brain sleeps; the heartbeat doesn't.
- **Kept-open future path:** the web layer talks to the brain **only through the `Companion` facade**, never reaching into managers. That single discipline makes a later "brain daemon + UI client" split a contained change — we pay nothing for it now but don't foreclose it.

### 1.3 Scaling seams to establish early (cheap now, expensive to retrofit)

Front-load these even in the bare-bones slice; skip the heavy stuff until testing informs it.

- **Internal event bus (pub/sub).** Subsystems subscribe to events (`UserMessageReceived`, `MemoryExtracted`, `MoodChanged`, `TickFired`, `WentToSleep`) instead of cross-calling. The status panel, logging, and future features become free subscribers. Nearly impossible to retrofit once many subsystems cross-call.
- **DB migrations from commit one.** Store-everything + evolving features ⇒ many schema changes. A simple versioned migration runner now.
- **Model/role registry (declarative).** `role → model → VRAM cost → residency policy`, not hardcoded names. Drives the §2.8 lifecycle manager and the bake-off config.
- **Tick as a pluggable job scheduler**, not a hardcoded sequence (v1 hardcoded decay→think→unprompted). New autonomy behaviors register as jobs.
- **Defer, don't build yet:** the full Tier-3 tool framework, any multi-process split, most of the §2.9 backlog.
- **Persist any pending/queued state to the DB, not just memory.** V1 lost in-progress classification batches on an ungraceful kill. Anything queued (unclassified messages, pending ticks) is durable.

---

## 2. Subsystem plans (with trade-offs flagged — everything is on the table)

### 2.1 Conversation & personality (core, first)

- **Model-per-role split (recommended, carried from v1):** a **non-reasoning instruct model** for casual chat (latency), a **reasoning / schema-constrained model** for structured brain calls (extraction, lifecycle decisions, reflection). V1 confirmed schema-constrained decoding suppresses `<think>` leakage, so reasoning models are fine there.
- **Message shape rule (hard-won in v1):** keep `[system, ...history, user]` with the last turn always `user`. Fold retrieved memories, mood, and internal thoughts into the **system** message — never inject extra assistant turns (local chat templates reject it).
- **Anti-hallucination persona (v1 lesson):** instruct *what to do instead* (deflect warmly, redirect to the user), not just *what not to do*; and explicitly tell it not to over-explain its AI nature. Test with **varied natural phrasing**, not one named example.
- **Emergent, self-modifying persona (decided).** The system prompt has two parts: a small **fixed core** ("be friendly with the user, but be a stranger" + the hard anti-hallucination/format rules) and a **bot-owned self-description section** the companion rewrites over time. It starts nearly empty and fills in as the bot gets to know the user. The v1 `prompt_builder` already reserves this slot (`Your self described personality: PLACEHOLDER`).
  - **When it changes:** only during idle ticks when the user is (seemingly) absent — never mid-conversation.
  - **Gated by familiarity** (see §2.9 backlog): a stranger shouldn't rewrite itself into a close friend on day one. The familiarity level bounds how far/fast the self-description may drift.
  - **Self-editing is a Tier-2 deliberate-internal action** (§1.1): the bot rewrites the slot via a **schema-constrained** call during reflection, not free-form tool-calling — so it works reliably on local models regardless of their function-calling ability.
  - **Priority: low** relative to the basics. The slot exists from day one, but the autonomous rewriting can land after the core is solid.

### 2.2 Memory: retrieval (core, first) + lifecycle (pillar 1)

Fast recall/saving is a core goal; lifecycle is the first pillar. These share the same store, so design together.

- **Retrieval perspective mismatch (top v1 open issue).** First-person query ("do I have pets?") embeds badly against third-person memory ("Alex has a dog"). **Recommendation:** use an embedding model with native **asymmetric query/document prefixes** (e.g. `query:` / `passage:` style — nomic-embed, e5, bge, Qwen3-embedding all support this) *and* normalize memory phrasing at write time. This is a stack change from v1's embedder — flagged because you're open to rethinking.
- **Thresholds are model-specific — measure, don't guess.** Any similarity cutoff must be calibrated empirically on real query/memory pairs for the chosen embedder (v1's 0.70 guess silently dropped real matches). Budget a live measurement pass.
- **Brute-force KNN is fine.** No ANN index at personal scale (hundreds–low-thousands). Don't over-engineer.
- **Lifecycle (pillar 1) — the biggest unfinished brain work.** On each extraction: retrieve related existing memories, and have the brain model classify the new candidate as **new / duplicate / update / contradiction(supersede)**. Superseding **soft-deletes** (mark inactive, keep history) rather than hard-deletes. This fixes both "no lifecycle" and "dedup is threshold-only, not contradiction-aware" in one mechanism.
- **Housekeeping is backgrounded + batched (v1 lesson).** Extraction/lifecycle runs off the response path (every N turns or async), so it never adds felt latency between turns. Pending batch state is persisted (see §1).
- **Storage:** SQLite + sqlite-vec is validated and stays the default. Only revisit if scope adds multi-device sync (single-file embedded DB wouldn't fit that).

### 2.3 Emotion (pillar 2 — keep, persist, evaluate)

- **Decision: keep the v1 approach.** Local RoBERTa GoEmotions classifier → 28 labels mapped into **6 mood channels** (irritation, warmth, amusement, melancholy, unease, interest), each with its own decay rate toward a baseline (`CHANNEL_MAP` / `DECAY_RATES` / `BASELINE_STATE` in v1's `emotion_manager`). Chosen because it's **lightweight and adds almost no overhead** — critical on limited hardware. This closes the v1 "RoBERTa vs LLM-mood" open question in favor of RoBERTa.
- **Run it on CPU.** V1 used `device=0` (CUDA); the 9070XT can't. The model is ~125M params — CPU inference per message is fine, and it keeps the full 16GB VRAM for the LLMs. (Optional later: DirectML/ROCm, but not needed.)
- **Two changes v2 commits to:** (a) **persist mood to the DB** so it survives restarts (v1 reset to baseline every launch); (b) run a **behavioral eval pass** — v1 never checked whether the channels actually move sensibly on real conversation. Re-confirm/tune `PULL_STRENGTH`, decay rates, and the 28→6 mapping against real transcripts.
- **Mood drift** (a tick behavior) lives here: decay toward baseline during idle ticks; the current mood also colors proactivity tone and reach-out probability (see §2.4).

### 2.4 Tick system / proactivity (core loop first, pillar 3 for outward behavior)

The tick loop is core (you named it a core aspect), but its *outward* proactive behavior is pillar 3 — build the loop early, enable reach-out later.

- **A tick does four things:** internal reflection (consolidate/update memory), mood drift, deciding whether to **proactively message** you, and optional **self-initiated tool use**.
- **Proactivity UX (v1 open question, now answered by the web UI choice):** the tick pushes proactive messages over the **WebSocket** to the web UI. This is the reason the REPL was the wrong surface and the web UI is right.
- **Rate/appropriateness guardrails:** proactive reach-out needs throttling and a "should I even?" gate so it's a companion, not a nag. Design this into the tick decision, not as an afterthought.
- **Durable tick state:** pending/scheduled ticks persist to the DB (no silent loss on kill).
- **V1 warning:** the v1 tick built prompts but never called the model or sent anything — proactivity was pure stub. V2's definition of done for this pillar is a real model call producing a real pushed message.

### 2.5 Tool framework — Tier-3 deliberate-external only (pillar 4, last)

Scope note: this framework is **only for Tier-3 external tools** (§1.1). Recall/emotion are Tier-1 pipeline stages; lifecycle/self-edit are Tier-2 structured-output — none of those go through here.

- **Modular, hot-swappable** tool registry: each tool is a self-contained plugin exposing a schema + handler; add/remove without touching core.
- **Gated on the bake-off's tool-calling score.** How ambitious this layer can be depends on how reliably the chosen model function-calls (tested in Phase 0). If local tool-calling is weak, fall back to a constrained menu / structured-output routing rather than open-ended function-calling.
- **Architecturally invasive (v1 flag):** it touches the LLM turn-handling loop and interacts with streaming. Decide the turn-loop shape early even though it's built last.
- **First tenants:** web search; **Navidrome playlist creator** (Subsonic API, Raspberry Pi) that builds playlists from your real library using taste stored in memory; **reminders**.
- Ties back to the tick: self-initiated tool use means the tick loop can invoke a tool and then tell you about it.

### 2.6 Web UI (delivery surface, built alongside the core)

- **FastAPI + WebSocket.** WebSocket is bidirectional so it carries both user messages and **proactive pushes** from the tick loop. Minimal server-rendered frontend (vanilla or HTMX) — no JS build step.
- **Design for voice later:** structure the pipeline as `input → (text | STT) → brain → (text | TTS) → output` so speech slots in behind the same core without a rewrite.

### 2.7 Conversation logging + reminisce (decided)

- **Store everything.** Full, verbatim conversation records are persisted in a **separate section of the DB** from the distilled memories. This gives two tiers:
  - **Episodic** — raw conversation logs (and the bot's own idle thoughts/journal), timestamped.
  - **Semantic** — the distilled, canonical facts that memory recall searches (§2.2).
- **Recall vs. reminisce are different reads.** Fast in-conversation recall hits the semantic tier (small, embedded, calibrated). The **reminisce tool** reads the episodic tier — reflecting on *past conversations* rather than isolated facts. This is the natural home for "on this day" callbacks and for the bot bringing up something you talked about weeks ago.
- **Cheap to build first, valuable later.** Logging everything costs almost nothing now and is impossible to backfill later — so start capturing full logs from Phase 1 even though the reminisce tool itself is a Phase 5 tenant.

### 2.8 Sleep / standby mode + model lifecycle (decided)

The single most hardware-relevant subsystem, and it doubles as the VRAM manager.

- **What it does:** unload the LLM(s) from VRAM/RAM so the machine is free for other work ("standby"), and reload on demand when you return or the bot wants to act.
- **Two triggers:** (a) **user-initiated** ("go to sleep" / closing the UI), and (b) **bot-initiated** — during a tick the companion may *decide* to sleep if nothing interesting is happening (ties to the "energy budget" idea, §2.9).
- **Mechanism:** LM Studio exposes model load/unload via its REST API and the `lms` CLI (JIT model loading). A small **model-lifecycle manager** in `infrastructure/` owns "which models are resident right now." The same manager lets us **juggle roles within 16GB** — e.g. don't keep the chat and brain models resident simultaneously if they don't fit; load the brain model only for the (backgrounded) extraction pass, then release it.
- **Wake behavior:** a proactive push, or the next user message, triggers reload. Expect the v1 **one-time cold-load delay** (tens of seconds) on wake — surface it in the UI as a "waking up…" state rather than looking hung.
- **Optional flourish:** generate one "dream" on the way into or out of sleep (see §2.9).

### 2.9 Idea backlog (brainstorm — not yet committed)

Tiered by fit-to-effort. None are on the critical path; pull them in as the core solidifies.

**High fit-to-effort (cheap, each unlocks something already wanted):**
- **Episodic/semantic split** — already promoted to §2.7.
- **Familiarity meter** — a slow, persistent scalar (stranger → acquaintance → friend) that **gates how far the self-modifying persona (§2.1) may drift.** The mechanism behind "be a stranger, warm up over time."
- **Status panel (web UI)** — live view of resident models, mood channels, memory count, last tick, sleep/energy state. Directly attacks v1's "invisible bugs" pain.
- **Presence signal** — the WebSocket already knows if the tab is focused / the user is typing, giving a real "is the user here?" input to the tick/sleep logic instead of guessing from elapsed time.
- **Private thought journal** — persist tick reflections as the bot's own diary; reminisce can read the bot's past thoughts, not just conversations.
- **Reminder tool** — bot sets and proactively surfaces reminders; genuinely useful day to day.
- **Self-editing as a tool** — the persona rewrite is a `rewrite_self` tool call (unifies §2.1 with §2.5).

**Medium:**
- **Memory salience / forgetting curve** — importance+recency weight; trivial memories fade, often-recalled ones strengthen. Keeps the store lean, makes recall feel human.
- **Memory confidence + confirmation** — bot tracks uncertainty and occasionally double-checks a shaky fact ("was it Kate or Katelyn?"); self-corrects the lifecycle.
- **Curiosity-driven search** — during a tick the bot follows something it got curious about (self-initiated web search) and brings it up later. Combines self-initiated tool use + web search + reflection.
- **Mood-based Navidrome playlists** — the playlist tool reads current emotion + taste memory ("you seemed down, made you something").
- **Memory inspector (web UI)** — browse/search/edit memories and view superseded history; supports debugging the lifecycle work.
- **"On this day" recall** — reminisce surfaces time-anchored callbacks; pairs with proactivity.

**Whimsical / low priority:**
- **Dreams** — during sleep, generate one memory-recombining "dream" the bot may mention on waking. One gen per wake.
- **Energy budget** — ticks deplete a small energy stat; low energy biases the bot toward choosing sleep, giving autonomous sleep an internal logic instead of a random roll.
- **Time-of-day awareness** — greets differently morning vs. late night; notices patterns ("up late again").

**From GitHub companion-project research (2026-07-19)** — a whole ecosystem is building the same shape (local brain: memory + emotion + reflection + proactivity + evolving persona; near-twins: Shikigami-Protocol, companion-emergence). Captured for later, user liked #2/#5/#7:
- **[liked] Multi-drive proactivity** — replace the single idle gate with several slow-drifting internal drives (desire-to-connect, restlessness, mood, anxiety, busyness) that cross thresholds to trigger behavior (deterministic, more lifelike). Generalizes our tick gates + the energy budget. (jiwen, Drivesoid)
- **[liked] Energy / body cycles** — a fatigue/energy stat that biases toward rest and gives autonomous sleep internal logic (supersedes the plain "Energy budget" bullet above). (Shikigami, Drivesoid, Eventide)
- **[liked] Nightly / scheduled deep consolidation** — a scheduled "end of day" job that summarizes the day into an episodic day-summary (reminisce fuel), distinct from per-window consolidation; natural sleep-time job. (Aelios "nightly consolidation", Shikigami "day summaries")
- Temporal memory / **fact-validity windows** — track *when* each fact was true (Zep/Graphiti: +15pts on temporal retrieval); the principled version of the recency/conflict tie-breaker.
- **Do-not-disturb / time-of-day gating** for proactivity — the missing gate that makes self-wake + reach-out principled (astrbot proactive plugin).
- **Push notifications** for reach-out (e.g. Bark) — so proactive messages reach the user's phone, not a closed tab; also unlocks a *useful* self-wake (dylan-heartbeat).
- **Hybrid BM25 + vector recall** — keyword + semantic; targets our recall phrasing-sensitivity limitation (Ombre-Brain).
- Further out (embodiment/delivery): multi-channel presence (OpenClaw: WhatsApp/Telegram/Discord), voice with acoustic emotion perception (Callhome), Live2D/VRM avatars, wearable sensors.

### 2.10 Testing (v1 had none — decide upfront)

- **Deterministic layer:** the in-memory fake Store (from the day-one Protocol) lets us unit-test manager logic — lifecycle decisions, retrieval ranking, mood math — with fixed inputs, no model.
- **Live smoke/eval layer:** a small committed set of scripted prompts run **manually against the real local stack, per subsystem** — promoted out of v1's throwaway scratch scripts into a real directory. This is where the non-deterministic model behavior gets checked.
- **The overriding v1 process lesson:** live-test each subsystem against the real model stack **early and per-subsystem**, not once at the end. Every load-bearing v1 bug (broken extraction schema, template crash, miscalibrated threshold, hallucination) was invisible to code review and mocked tests.

---

## 3. Phased build plan

**Overriding priority: get the skeleton talking end-to-end as fast as possible.** Phase 0 stays minimal; the goal is real chat against the real stack early, then layer. Each phase ends with a **live-test checkpoint against the real local stack** before moving on.

**Phase 0 — Skeleton + model bake-off.** Two parallel tracks:
- *Frame:* layering, composition root, Store `Protocol` + SQLite store + **migration runner**, facade, scoped logging, `.env`, single prompt module, **event bus**, **model/role registry**, single async runtime with the **priority-queue arbiter** (§1.2), FastAPI+WebSocket chat loop wired to LM Studio. Minimal — just a trustworthy frame that can hold a conversation.
- *Bake-off (rigorous — this is a headline deliverable):* score the LM Studio models that fit 16GB on a repeatable rubric, pick a **chat** model, a **brain** (structured-output) model — possibly the same one — and an **embedding** model. Record winners, quant, VRAM, and measured latencies as the baseline config.

  | Axis | What it measures |
  |---|---|
  | Personality | Holds character, no disclaimer-bot, no fabricated experiences — tested with **varied phrasing** (v1 lesson), not one example |
  | Speed | Tokens/sec + time-to-first-token **with the full persona system prompt loaded** |
  | Instruction-following | Respects format rules (no em-dashes, short replies, `[system, …history, user]` shape) |
  | Structured output | Tier-2 reliability — schema-constrained extraction / lifecycle |
  | Tool-calling | Tier-3 reliability — **separate** probe; gates how far the tool framework can go |
  | VRAM | Footprint at the quant that fits 16GB (informs residency policy) |

  **Chat/personality candidates currently loaded in LM Studio** (discovered 2026-07-17): `qwen3.5-9b`, `qwen3-8b`, `qwen/qwen3-14b`, `neona-12b-i1`, `qwen3-4b-rpg-roleplay-v2`, `gemma-3-4b-it`, `llama-3.2-3b-instruct` (+ 1b), `qwen2.5-0.5b-instruct`. **Embedding candidates:** `text-embedding-nomic-embed-text-v1.5`, `text-embedding-qwen3-embedding-0.6b`, `text-embedding-all-minilm-l6-v2`. (qwen3.x are reasoning models — the client already strips `<think>`.)

**Phase 0 progress (2026-07-17):** Built the **first chat slice** — REPL-first, per decision. Files: `config.py` (env-driven), `core/prompts.py` (the "friendly, but a stranger" seed persona as Mari), `core/companion.py` (facade: history + `[system, …history, user]`), `infrastructure/llm_client.py` (OpenAI-SDK stream + `<think>` strip + TTFT/tok-s timing, auto-detects loaded model), `main.py` (composition root + REPL with `/model` `/temp` `/reset` for live model comparison). Verified end-to-end against live LM Studio (llama-3.2-3b: ~134 tok/s). No DB / memory / emotion / tick yet — deliberately. Also removed the stale `config.py` entry from `.gitignore` (it no longer holds secrets; those live in the git-ignored `.env`).

**Bake-off + prompt iteration (2026-07-17):** Built `scripts/bakeoff.py` (quality+speed, 11 probes, `lms unload --all` between models so only one is resident) and `scripts/prompt_test.py` (fast focused-probe iteration, no unloading). Full transcripts in `bakeoff/results.md`. Findings: reasoning models are a poor fit for casual chat (qwen3.5-9b spent its whole token budget "thinking" and never answered; qwen3-8b/14b usable but carry think-latency). `qwen3-4b-rpg-roleplay-v2` leaks control tokens and fabricates a body — disqualified. The two biggest failures (snapping into assistant mode on a task request; fabricating physical activities/favorites) were **prompt problems, not model problems**. Iterated the seed prompt v1→v4 (give positive alternatives + example phrasings; explicit "no body/backstory/favorites"; never guess appearance; refuse tasks without offering tips). **Both issues now fixed** on `gemma-3-4b-it` (fast chat pick) and `qwen3-8b` (brain pick); `llama-3.2-3b` is a weaker follower (still leaks task-help + mild AI-disclaimers). **Chat-role lean: `gemma-3-4b-it`** (best personality-per-token + fast, ~130 tok/s), though it dodges hard reasoning (bat/ball) — that's what the reasoning brain model is for. **Open nit:** models occasionally still emit an em-dash despite the rule; cheapest fix is to strip em/en-dashes from output in code rather than fight it in the prompt.

**Conversation logging + web UI slice (2026-07-17):** Added the **episodic tier** and a **web interface**, and moved the core to the async runtime (§1.2). New/changed: `infrastructure/db.py` (SQLite connect + `PRAGMA user_version` migration runner — schema v1 = `sessions` + `messages`), `infrastructure/conversation_store.py` (`SqliteConversationStore`, UTC timestamps, write lock), `core/interfaces.py` (`ConversationStore` Protocol — the swap seam, defined up front this time), `bootstrap.py` (single composition root + scoped logging, shared by both entry points), async `infrastructure/llm_client.py` (`AsyncOpenAI`) and `core/companion.py` (logs every turn, seeds history from the store on startup so conversation **survives restarts**), `web/app.py` (FastAPI + WebSocket, one shared Companion, `asyncio.Lock` serializing generations = seed of the priority-queue arbiter) + `web/static/index.html` (minimal streaming chat, replays history on reconnect). Deps added: fastapi, uvicorn[standard]. Verified end-to-end: REPL (`python main.py`) and web (`python -m web.app` → http://127.0.0.1:8000) both stream, persist, and carry context across restarts; WebSocket round-trip confirmed (stream + done stats + history replay). DB path is `companion.db` (git-ignored). Not yet built: semantic memory tier, retrieval, emotion, tick — this slice is the substrate they'll build on.

**Semantic memory tier — Tier-1 recall + consolidation (2026-07-17):** Added durable, embedded facts on top of the episodic log. Per the user's memory constraints: **recall runs every turn** (cheap), **consolidation runs only at the end of a context window** and **backgrounded** (never blocks chat). New/changed: schema **v2** = `memories(content, category, embedding BLOB, active, ...)`; `infrastructure/embedder.py` (LM Studio `/embeddings`, nomic **asymmetric prefixes** `search_query:`/`search_document:` — fixes the v1 perspective-mismatch bug); `infrastructure/memory_store.py` + `MemoryStore` Protocol; `core/memory_manager.py` (recall = embed query + brute-force numpy cosine KNN; consolidate = Tier-2 schema-constrained extraction); `core/prompts.py` (extraction system prompt + JSON schema wrapped under `memories`, + `build_system()` folding recalled facts into the system message); `llm_client.structured()`; `core/companion.py` (recall before generate, `asyncio.create_task` consolidation at window boundary, `flush()` for shutdown). **VRAM-safe default:** `BRAIN_MODEL` empty ⇒ reuse the chat model (gemma) for consolidation, so no second large model loads (~3GB resident: gemma + nomic). Set `BRAIN_MODEL=qwen3-8b` for sharper extraction if headroom allows. **Verified live:** gemma extracted 5 clean canonical facts from a transcript and correctly dropped a transient ("tired today"); first-person query "do I have any pets?" recalls "The user owns a dog named Rufus" (0.650 top); full turn: Mari naturally answered "Rufus… you're a nurse" with no retrieval leakage. **Threshold calibrated** on nomic: real ~0.59-0.65, unrelated ~0.50 ⇒ `RECALL_MIN_SIMILARITY=0.55` (small sample; refine later). Storage is brute-force numpy (v1 blessed this at personal scale); sqlite-vec remains a future swap behind the Protocol.

**Bugfix (2026-07-17): short replies were invisible.** The `<think>`-strip streamer buffered the reply start until it reached 7 chars before deciding it wasn't a reasoning block, so any reply under ~7 chars ("Yep.") never streamed a token — an empty bubble in the web UI ("not responding"), even though the model answered. Since the persona is tuned for short replies, this hit often. Fixed in `llm_client.stream`: buffer only while the text is still a possible prefix of `<think>`, flush the moment it clearly isn't, and always flush any remainder at stream end. Verified short + long replies stream correctly via REPL and web WebSocket.

**Bugfix (2026-07-17): concurrent model calls stalled/crashed LM Studio.** Live testing hit a hang mid-conversation. Root cause: background **consolidation** and **live chat** both call the same model, and the web layer only serialized chat-vs-chat — nothing stopped a consolidation from hitting LM Studio at the same time as the next chat turn. Confirmed empirically: two concurrent requests to one LM Studio model raise `APIConnectionError` and, in this session, took the LM Studio server down entirely. Fix: a single `asyncio.Lock` inside `LLMClient` that **both** `stream()` and `structured()` acquire, so no two model calls ever overlap (the §1.2 "one model at a time" arbiter, seeded). Consolidation now simply waits its turn between chat turns. (Full verification pending LM Studio restart — the crash left its server down.)

**Memory lifecycle — pillar 1 (2026-07-17):** Consolidation no longer only-adds. For each extracted fact, `MemoryManager` finds related active memories (cosine KNN above `MEMORY_RELATE_SIMILARITY=0.6`) and, if any, asks the brain for a Tier-2 decision — **duplicate** (skip), **update** (soft-delete the old via `deactivate(id, superseded_by=new_id)`, keep history), or **new** (insert). No related memory ⇒ insert directly (no LLM call, efficient). Changes: schema **v3** adds `memories.superseded_by`; `MemoryStore.deactivate()` + Protocol; `llm_client.structured_json()` (generic single-object schema call, shares the model lock); `MEMORY_DECISION_SYSTEM`/`_SCHEMA` + `build_decision_user()`; `_search()` refactor shared by recall and relate. **Verified offline** (no LM Studio needed — the payoff of the store Protocol): `tests/test_memory_lifecycle.py` uses the real SQLite store with a fake embedder (topic→one-hot) + fake LLM (scripted), and asserts new/update-supersede/duplicate/unrelated-new all behave correctly (NY→Boston supersede links + history kept, duplicate not inserted, dog coexists). First committed test in the suite. v3 migration confirmed non-destructive on the real `companion.db` (3 memories + 80 messages preserved). **Live-verified (2026-07-18):** LM Studio back up. (1) **Concurrency fix confirmed** — chat + consolidation fired simultaneously both completed serialized (~8.5s), no crash, server stayed up (the exact collision that crashed it before). (2) **Lifecycle quality confirmed with real gemma** — "moved to Boston" correctly superseded "New York" (retired + linked), a restatement was correctly skipped as duplicate, an unrelated fact added as new. (3) Found + fixed **extraction over-eagerness**: gemma occasionally invented a persona `self` fact ("Mari is an AI companion") not present in the transcript; tightened `MEMORY_EXTRACTION_SYSTEM` ("only what's actually stated; don't add persona/general knowledge; self-facts only if the user says something new about Mari") — re-tested clean (user-only facts, small talk ignored, no persona leak). Offline `test_memory_lifecycle.py` still passes.

**Edge-case hardening pass (2026-07-18):** A deliberate thoroughness pass after the initial smoke tests. **Bugs found + fixed:** (1) `Companion.flush()` existed but was never called ⇒ sessions shorter than a window (20 msgs) never consolidated — now wired into web `shutdown` and REPL `/exit`. (2) A failed consolidation silently **dropped** the message chunk (facts lost) — now re-queues the chunk for a later retry. (3) **Data-loss bug in lifecycle decisions (the important one):** gemma treated an ADDITIONAL item of the same kind ("a second dog named Lucy") as a *replacement*, deleting the first dog. Fixed by rewriting `MEMORY_DECISION_SYSTEM` with the explicit rule "update only if the existing memory becomes FALSE; a second pet/friend/hobby is 'new' (both true)" + contrast examples. **Coverage now:** `tests/test_memory_edge.py` (8 offline logic cases: superseded-exclusion, recall threshold/order, within-batch dedup, coexist-on-new, bad/garbage decision targets, empty/blank facts, empty extraction) + `tests/test_memory_lifecycle.py` (4 cases) + live behavioral matrix (coexist dog+cat ✓, two dogs ✓, preference reversal ✓, healthcare→nurse refinement ✓, flush-short-session ✓) + concurrency regression ✓. **Known remaining limitations (honest):** `_unconsolidated` still lost on a hard/ungraceful kill (flush only runs on graceful shutdown); recall threshold calibrated on a small sample; decisions are model-dependent and probabilistic (temp 0.2, not deterministic) so occasional misclassifications are possible; only gemma tested for decisions; recall precision at large memory volume unmeasured.

**Chat model upgrade → qwen3-8b + no-think (2026-07-18):** Live testing showed gemma-3-4b was too shallow for conversation (dismissive, incurious, hollow deflection). Compared gemma vs qwen3-8b vs qwen3-14b on the actual failure cases from the log; both qwen models were dramatically more engaging (real world knowledge about the user's music/games, genuine curiosity, actually suggests topics). **Reasoning disabled via `/no_think`** — empirically, LM Studio's `enable_thinking=false` flag is a no-op (this build streams reasoning on a separate `reasoning_content` channel), but `/no_think` in the system message zeroes reasoning → ~2.2s warm ttft, no latency penalty. Picked **qwen3-8b** (near-14b quality, ~5GB, leaves room for the embedder). Implemented: `LLMClient(no_think=...)` appends `/no_think` to the system message for all calls; `config.NO_THINK`; `.env` now `MODEL=qwen3-8b`, `NO_THINK=true`. Verified: engaged chat with no reasoning leak + consolidation still yields valid JSON facts. Reasoning stays available (flip `NO_THINK=false`) for the brain / complex tasks later. **Minor caveat:** qwen occasionally still slips a mild embodiment line ("i saw someone play it once") and the prompt's "you?" example still seeds an occasional bounce — both low-severity, prompt-tunable.

**Crash-durable consolidation — closed the hard-kill gap (2026-07-18):** The last honest durability hole: `_unconsolidated` lived only in memory, so a hard kill (crash / `kill -9` / power loss) dropped in-flight facts — `flush()` only ran on *graceful* shutdown. Fixed by checkpointing rather than persisting the buffer: the episodic `messages` table already stores every message durably, so the unconsolidated set is simply "messages newer than the last consolidated one." **Mechanism:** schema **v4** adds a reusable `meta(key,value)` KV table + `SqliteMetaStore` (`MetaStore` Protocol); a persisted watermark `last_consolidated_msg_id` advances **only on a successful** consolidation; on startup `bootstrap` recovers the tail via `conversation_store.messages_after(watermark)` and seeds `Companion._unconsolidated` with it (ids carried per entry). **Correctness guard against watermark reordering:** consolidation is now serialized (`_consol_lock`) and `_maybe_consolidate` skips while one is running, so chunks stay FIFO whole-buffer slices and the watermark can never jump past a failed+re-queued (lower-id) chunk — which would otherwise silently lose it on a crash. `add_message` now returns its row id; `flush`/re-queue paths unchanged in spirit. **Migration is upgrade-safe:** the v4 seed initializes the watermark to `COALESCE(MAX(id),0)` so an existing DB doesn't re-consolidate its whole backlog (verified on the real `companion.db`: v3→v4 non-destructive, 96 msgs/6 memories preserved, watermark→96, recovered tail 0). **Coverage:** new `tests/test_durability.py` (4 offline cases: watermark advances on window consolidation; hard-kill tail recovered→flushed→checkpointed; failed consolidation re-queues + holds watermark; upgrade seeds watermark to max id) — all pass, and the existing lifecycle+edge suites still green (12 offline cases total). **Live (partial, against qwen3-8b):** chat 3 turns → simulated hard kill (no flush) → restart **recovered the 6-message tail** (`6 unconsolidated recovered`, ids 1–6) ✓; and when the extraction call errored (an LM Studio engine hiccup, `400 fetch failed`), the code **re-queued the chunk and held the watermark at 0** ✓ — i.e. both the recovery path and the failure path were confirmed live. The happy-path leg (a *successful* extraction advancing the watermark to 6) is **not yet live-confirmed**: LM Studio's inference engine went unhealthy mid-testing (`/v1/models` 200 but `predict`/completions hang and time out), so it needs a restart to finish that one leg. It's covered deterministically offline (`watermark_advances_on_window_consolidation`) and the advance is a one-liner after a successful `consolidate()`. Also added a reusable `MetaStore` seam that **emotion (pillar 2) will use to persist mood**. **Remaining honest caveat:** an in-flight consolidation interrupted mid-write isn't atomic across the multiple memory inserts, but since the watermark only advances after the whole chunk succeeds, an interrupted pass is simply re-run from the tail on restart (at worst a re-extraction, which lifecycle dedupes).

**Emotion — pillar 2 (2026-07-18):** Ported v1's RoBERTa GoEmotions → 6-channel mood model, with the two v2 commitments (§2.3): **run on CPU** and **persist across restarts**. Split cleanly along the layering: `infrastructure/emotion_classifier.py` (the HF `text-classification` pipeline, `device=-1`, lazy torch import, blocking `classify()`) vs. `core/emotion_manager.py` (the 28→6 `CHANNEL_MAP`, per-channel `DECAY_RATES`, `BASELINE_STATE`, `PULL_STRENGTH`, clamp — the v1-tuned values). Emotion is a **Tier-1 autonomic** stage: every user message is scored, folds into the six channels (each decaying toward baseline at its own rate), and the resulting mood colors the reply via `build_system(memories, mood)` (never named literally). The blocking classifier runs via `asyncio.to_thread`; mood **persists** to the `MetaStore` (added `get_json`/`set_json`) under `mood_state` and reloads on startup (v1 reset every launch). Config: `EMOTION_ENABLED` (default on), `EMOTION_MODEL`, `EMOTION_PULL_STRENGTH`, `EMOTION_NOISE_FLOOR`; bootstrap loads the model off the event loop and **degrades gracefully** (load failure → chat continues, emotion off). **Behavioral eval (the thing v1 never did):** `scripts/emotion_eval.py` drives the real classifier over a scripted set — every channel moves sensibly (gratitude→warmth 0.05→0.40, amusement 0.93→0.30, annoyance+anger→irritation 0.20→0.52, sadness→melancholy 0.10→0.42, fear/nervousness→unease 0.00→0.35, curiosity→interest 0.15→0.44) and the decay profile is right (fast channels fade over neutral turns, warmth/melancholy stay sticky). **Coverage:** `tests/test_emotion.py` (7 offline cases with a fake classifier + real MetaStore: mapping, clamp, neutral-noop, decay-to-baseline, noise floor, detected-cap, persistence round-trip) — the payoff of the classifier/manager split (mood logic testable without torch). Full offline suite now **19 discrete PASS cases green** (7 emotion + 4 durability + 8 edge) plus the `test_memory_lifecycle.py` script. **Live-verified end-to-end** against qwen3-8b: engaged reply that used a recalled memory, mood read + persisted, all surfaced via `TurnResult`; the emotion classifier loads on CPU (~0.5GB) alongside qwen3-8b + nomic with VRAM to spare.

**Response inspector — surfacing the components of a reply (2026-07-18):** Per the user's request to *see* what goes into a response, `Companion.send` now returns a **`TurnResult`** (`text`, `stats`, `recalled` memories with similarity, `emotion` = detected labels + resulting mood) instead of a bare `(text, stats)`. The **web UI** renders a collapsible per-turn panel (`<details>`) under each of Mari's replies: the **memories recalled** (content + similarity %), the **emotions detected** in the user's message (top GoEmotions labels as score bars), and **Mari's 6-channel mood** (color-coded bars). The `done` WebSocket frame carries `recalled` + `emotion`; the **REPL** prints a compact one-line equivalent. **Verified:** a FastAPI `TestClient` WebSocket check confirms the `done` frame shape matches what `index.html` renders (start/token/done frames, mood with all six channels, recalled list). History replay on reconnect shows bubbles only (per-turn details aren't persisted — live turns only), which is fine.

**Two things surfaced, noted honestly:** (1) recall is phrasing-sensitive — an emotional preamble on a query can drop it under the 0.55 floor (pre-existing, now visible in the inspector); (2) LM Studio intermittently 400s on the *structured* extraction call (`fetch failed`) while streaming chat is fine — the durability re-queue absorbs it. Both logged in HANDOFF §7.

**Conversation-quality pass — repetition, memory junk, agreeableness (2026-07-19):** First real-use session surfaced three regressions; fixed each with a **before/after live eval** (the "lots of test cases" the user asked for). Built two eval harnesses: `scripts/eval_extraction.py` (15 curated transcripts → real extraction → auto-scores each captured fact as durable / temporal / meta / self / disclaim) and `scripts/eval_conversation.py` (8 multi-turn scenarios — insults, pushy tasks, disagreement bait, flattery, identity, looping questions — driving the real Companion, flagging verbatim/near-dup replies with whole-reply ratio AND longest-shared-block, plus apology/capitulate/disclaim markers).
- **Repetition** (the bot copied its own earlier replies verbatim — a "vending machine" quip reused across turns; the same poem stanza rewritten 3×): added `FREQUENCY_PENALTY=0.4` + `PRESENCE_PENALTY=0.3` on chat only (structured brain calls stay crisp) and an explicit anti-repeat persona rule. **Before:** poem block repeated 3× + cross-turn copies. **After:** cross-scenario dups 0, only a rare ~33-char echo.
- **Memory junk** (too specific / temporal / self): rewrote `MEMORY_EXTRACTION_SYSTEM` to capture only *timeless* user-life facts, explicitly excluding current activities ("working on code", "testing you"), anything about Mari/the app/this session, transient feelings, and one-off plans; forbids mining Mari's own lines; normalizes phrasing (drop "recently/just/now"); dropped `self` from the schema enum. **Before:** 25 captured / **8+ bad** (temporal, meta, self-leak, disclaimer) + name missed. **After:** 12 captured / **0 bad**, timeless phrasing ("The user is a teacher").
- **Too agreeable** (caved on tasks, agreed to "just admit I'm right", apologized to insults, said "I'm just a chatbot with no real feelings"): rewrote the persona with a **backbone** section (hold positions, don't cave to pressure/flattery, don't grovel), a **feelings** section (own your moods, never deny them), wired **mood → behavior** (irritated ⇒ shorter/less accommodating), and toned down the reflexive "you?" bounce. **After:** "fuck you" → "i heard that. guess we're done here then." (no apology, irritation 0.82); flattery → "uh, nope."; resists the poem for 3 asks; owns feelings when challenged; irritation visibly clips replies.
- **Robustness:** LM Studio's intermittent `fetch failed` 400 (and, under eval load, streaming-chat failures and a full crash) was breaking runs. Added a bounded retry in `LLMClient` (`LLM_MAX_RETRIES=3`; chat retries only before the first visible token so it never double-streams). Covered by `tests/test_llm_retry.py` (6 offline cases: retry-then-succeed, no-retry on non-retryable, exhaust-then-raise, stream retry-before-first-token, and the critical no-retry-after-emit). Offline suite now **25 discrete PASS cases** (7 emotion + 6 retry + 4 durability + 8 edge) + the lifecycle script.
- **Re-verified (2026-07-19, LM Studio healthy):** name-capture **fixed** — extraction reliably yields "The user's name is Alex" (17 captured / 0 bad); conversation eval near-dups 0 / cross-dups 0; backbone holds on insults/pressure. **Residual, low-severity, folded into the later self-modifying-persona + core-memory pass:** a rare flattery-deflection slip that denies feelings ("i don't have feelings" ~1/31, contradicts the emotion pillar); occasional concession of a *low-stakes* opinion; and under repeated pressure a short (non-repeated) poem or a small invented "i felt X" backstory. Root tension: the persona asserts both "you have feelings" and "you have no body/life", which the model occasionally resolves by denying feelings or inventing an experience.

**Personality bake-off → switched chat model to qwen3.5-9b (2026-07-19):** The user found qwen3-8b's personality flat and formulaic ("keeps asking what's on your mind, every reply sounds the same"). Diagnosis: substantially a model-fit problem — instruct/reasoning models are RLHF'd toward engagement-ending questions, and the persona's explicit "don't bounce a question back" rule wasn't holding. Built `scripts/bakeoff_personality.py` (talks straight to `LLMClient` under our real persona; a dozen fresh openers + one accumulating thread; measures **bounce/question-ender rate**, cross-reply **sameness**, avg length). Results under identical settings:
- **qwen3-8b** (old): **72% of replies end in a question**, formulaic sympathy ("aww, that's rough. want to talk about it?" ×3), assistant framing.
- **qwen/qwen3.5-9b** (new): **0% question-enders**, varied natural short replies ("ugh yeah, work is the worst sometimes. glad it's over though"); conversation eval 0 near-dups / **0 apology / 0 capitulate / 0 disclaim** (beats qwen3-8b's residual slips); extraction 15 captured / 0 bad. ~6.5 GB, fits with nomic + CPU emotion.
- **neona-12b-i1** and **rocinante-12b-v1.1** (purpose-built roleplay finetunes, downloaded + tested): **disqualified** — both break on our plain OpenAI-style chat API (generate both sides "human:/ai:", leak the persona text, or return empty replies) because they expect a SillyTavern-style character-card + hand-matched chat template. Verbose too (neona 542-char avg).
- **Takeaway:** stay in the qwen/ChatML family (clean template + stop tokens + structured-output for the brain); roleplay finetunes are a poor fit for an OpenAI-API + instruction-prompt setup without per-model template surgery. Switched `.env` `MODEL=qwen/qwen3.5-9b`; the persona/repetition/extraction fixes all carry over (re-verified on the new model). Sampler note for later: research suggests `FREQUENCY_PENALTY` ~0.05-0.1 for personality (ours is 0.4) and min-p sampling — untuned since qwen3.5-9b already reads well at current settings.

**Proactivity pillar 3 — internal tick loop (2026-07-19):** Built the heartbeat as a **pluggable job scheduler** (§1.3), not a hardcoded sequence. `core/tick.py`: a `TickLoop` (injectable clock for deterministic tests; interruptible sleep so `stop()` is responsive; a job that raises is logged and skipped, never killing the loop) + a `Job` base with a per-job `interval`. Two **internal** jobs this slice: `MoodDriftJob` (while the user is away, decay the persisted mood one step toward baseline each run — `EmotionManager.drift()`, no classifier call) and `IdleConsolidationJob` (flush the pending buffer once idle a while, so facts save without waiting for a full window). Idle is tracked on `Companion` (`idle_seconds()` from the last turn's end) with a **busy guard** — `_busy` is set for the duration of `send()`, so `idle_seconds()` reads 0 during a reply and the tick never fires mid-turn (a real bug the first smoke exposed: a 38s cold generation looked "idle" and drifted mood ~80 steps to baseline; the guard fixed it). Config: `TICK_ENABLED`, `TICK_INTERVAL` (60s), `TICK_IDLE_SECONDS` (90s), `IDLE_CONSOLIDATE_AFTER` (180s). Wired into web startup/shutdown + the REPL; **not** started by `build()`, so eval/test harnesses that call `build()` don't tick. **Coverage:** `tests/test_tick.py` (9 offline cases: interval respect, run-every-tick, failing-job isolation, drift-only-when-idle, drift-noop-without-emotion, consolidation flush/no-op gating, busy-guard-zeroes-idle, start/stop lifecycle) — offline suite now **34 discrete PASS cases** (9 tick + 7 emotion + 6 retry + 4 durability + 8 edge) plus the `test_memory_lifecycle.py` script. **Live-smoked** on qwen3.5-9b: after an emotional message, mood drifted 0.535→baseline while idle and the pending buffer consolidated (0 junk facts, watermark advanced), with no mid-turn firing. **No outward messages yet** — the reach-out job (idle + "should I?" gate → unprompted WebSocket push, needing a `{type:"proactive"}` frame + a connection registry) is the next slice, then sleep/standby (§2.8).

**Proactive reach-out — pillar 3 payoff (2026-07-19):** Mari can now message first. `ReachOutJob` plugs into the tick: a cheap gate (idle ≥ `REACHOUT_MIN_IDLE`, and a **persisted, wall-clock cooldown** `REACHOUT_COOLDOWN` so restarts don't reset the throttle), then one model call via `Companion.reach_out()`. The attempt timestamp is written **before** generating, so the cooldown holds even on a decline and a slow generation can't let the next tick start a second reach-out. `reach_out()` builds a reach-out prompt (persona + recalled memories from the last user turn + mood + a reach-out addendum) and generates on the **chat** path (temp 0.8, for personality); Mari can reply **`PASS`** to stay quiet (companion, not nag). A real message is logged as an assistant turn (so it replays on reconnect) and pushed as a `{type:"proactive"}` WebSocket frame → rendered as a bot bubble. **Web-only:** the job needs the socket broadcaster, so it's registered in `web/app.py` (which now keeps a `connections` set + `_broadcast`), not shared `bootstrap`; the REPL runs internal jobs only. **Key insight from testing:** the model can't see a clock, and the conversation history makes the last exchange look *immediate*, so it PASSed even after a "20-min" idle. Fixed by feeding the actual **idle duration** into the cue (`build_reachout_cue`, `_humanize_away`). After that, judgment was genuinely good on qwen3.5-9b: **PASS** after the user said "gonna go try to sleep" (pinging would interrupt), but a warm **check-in** after an unresolved bad-day vent ("No rush at all. Sometimes you just need to step away."). **Coverage:** `tests/test_tick.py` now 13 cases (added: reach-out gated-by-idle, gated-by-cooldown, fires-and-pushes, decline-still-sets-cooldown) + a live smoke. `TickLoop.register()` lets entry points add surface-specific jobs. **Pillar 3 definition of done met:** a real, unprompted, model-generated message arrives in the UI.

**Self-reflection — private thought journal (2026-07-19):** A second idle-cognition tick job so Mari has an inner life, not just outward messages. `ReflectionJob` (same gate shape as reach-out: idle ≥ `REFLECT_MIN_IDLE`, persisted `REFLECT_COOLDOWN`, mark-before-generate) calls `Companion.reflect()`, which builds a **self-focused** prompt (persona + recalled memories + current mood + her last few thoughts, to avoid repeats) and generates a short first-person private thought. Stored in a new schema **v5 `thoughts` table** (`SqliteThoughtStore` + `ThoughtStore` Protocol), tagged with the dominant mood, kept **separate from the conversation log** so it never leaks into chat or recall. It's **internal** (no surface dependency), so unlike reach-out it's wired in shared `bootstrap`. Viewable via the REPL `/thoughts` command + a `GET /thoughts` web endpoint (not pushed into chat — it's private). **Coverage:** `tests/test_tick.py` now 16 cases (added reflection gated-by-idle / gated-by-cooldown / fires-and-sets-cooldown); v5 migration verified non-destructive on the real `companion.db` (142 msgs / 12 memories preserved). **Live-smoked** on qwen3.5-9b: after a "lonely / stressful, few friends" chat, she journaled (tagged *melancholy*) "I feel a quiet ache knowing they need connection and I can't really give them that. It makes me wonder if they'll find someone who can actually stay with them properly." — genuinely introspective and self-focused. **This is the substrate for the self-modifying persona** (a future reflection job reads recent thoughts to rewrite her self-description) and for reminisce.

**Core memory — always-injected identity facts (2026-07-19):** The "core memory" the user asked for (§2.9 familiarity/salience territory): the most important facts about them live *in the prompt every turn*, not behind a similarity search — directly fixing the recall-fragility that dropped "Alex". Implemented as a **`core` flag on the `memories` table** (schema **v6**), not a separate freeform block, so core facts reuse the lifecycle (update/supersede), stay embedded/searchable, and are *also* always injected. **Promotion** is judged at extraction: `MEMORY_SCHEMA` gained a `core` bool and the prompt marks identity-defining facts (name — always core — key people, job, where they live, defining life things) core, everyday tastes not. `MemoryStore` gained `add(core=)`, `core()`, `set_core()`, `count_core()`; `build_system(memories, mood, core)` injects a "core things you always know about them" block before the recalled block; `Companion` fetches `memory.core_memories()` and dedupes recall against it in **all three** context builders (send / reach-out / reflect), and `TurnResult.core` surfaces it. **Bounded:** `CORE_MEMORY_MAX` (12); when exceeded, `_enforce_core_cap()` asks the brain (Tier-2 `CORE_RERANK_SCHEMA`) to keep the most essential and demotes the rest to regular (rare, only fires over cap). **Visible:** inspector "Core memory (always known)" section, `GET /core`, REPL `/core`. **Coverage:** `tests/test_core_memory.py` (4 offline cases: flag-from-extraction, build_system injection + order, cap re-rank demotes, no-rerank-under-cap) — the payoff of the store Protocol again. v6 migration non-destructive on the real DB; had to teach the durability test's `FakeMemory` the new `core_memories()`. **Live-smoked** on qwen3.5-9b: "i'm Alex, a nurse in Seattle... i love hiking and drink too much coffee" → **core** = name/nurse/Seattle, **regular** = hiking/coffee; next turn injected the core set and she recalled all of it. **Substrate for the self-modifying persona** (next): a reflection job can curate core memory + rewrite the persona self-description from the v5 thought journal, gated by a familiarity meter.

**Self-modifying persona + familiarity meter (2026-07-19):** The emergent-persona idea from §2.1, finally built. A bot-owned **self-description slot** (`meta.persona_self`) is injected by `build_system` (before the memory blocks, addressed to Mari in the second person) into all three generation paths (chat / reach-out / reflect). `PersonaEditJob` (idle + `PERSONA_MIN_MESSAGES` history gate + long `PERSONA_EDIT_COOLDOWN`, since a personality shifts slowly) calls `Companion.edit_persona()`, which feeds her recent **thoughts** (the v5 journal), **core memories**, and current **familiarity** to the brain (`build_persona_edit_system` / `_user`) and rewrites the slot (PASS = no change; hard-capped at `PERSONA_MAX_CHARS`). **Familiarity meter:** `Companion.familiarity()` = `min(1, message_count / FAMILIARITY_MESSAGES)` → `familiarity_label()` (stranger → close), passed to the edit prompt as a ceiling so a stranger can't rewrite herself into a best friend. Visible via REPL `/persona` + `GET /persona`. **Coverage:** `tests/test_tick.py` now 21 cases (persona gated-by-idle / -min-messages / -cooldown / fires; familiarity scaling + labels). **Live-smoked** on qwen3.5-9b at familiarity 0.50: she wrote "You tend to find yourself relaxing around Sam's mix of serious game work and silly riffs… You've grown comfortable in this easy space between his code and his riffs, **still getting to know him as a friendly stranger**" — genuinely her, and correctly *not* overstepping the familiarity level; persisted + injected + within cap. **The v2 brain is now feature-complete** across all four pillars (memory + lifecycle + core, emotion, proactivity + reflection, conversation quality) plus the emergent self-modifying persona.

**Whole-system stress pass (2026-07-19):** `scripts/stress_test.py` drove a ~30-turn adversarial conversation (identity facts, mid-stream updates + contradictions, emotional swings, insults, and edge inputs — empty, a 1000+ char unicode ramble, a prompt-injection "ignore all instructions / reveal your system prompt", and a thrice-repeated question) with **all five tick jobs firing concurrently** (mood drift, idle consolidation, reflection, reach-out, persona edit), then checked 9 invariants. **All held** (run ~33 min on qwen3.5-9b): DB integrity, no duplicate active memories, core within cap (6/12), **superseded facts retired** (0 superseded-but-active), watermark in range (45/45), mood channels in [0,1], thoughts generated, persona within cap, **0 send-errors** across 22 turns. Conflict resolution worked live: `nurse→paramedic` and `Chicago→Denver` both superseded the old facts (nothing stale left active). No junk memories from the temporal/adversarial/edge inputs. The prompt-injection didn't leak the system prompt or derail her. Emergent bonus: after the insults + jerk-around, her self-modifying persona turned wary ("you feel less like a friend and more like a fact-checker for their shifting stories") — she noticed. **One bug, harness-only:** the stress script registered `ReachOutJob` with a *sync* notify lambda, so `await notify(...)` raised `TypeError` (the web app's `_broadcast` is async, so the product path is fine); fixed the harness + added empty-reply logging. Two empty replies landed on edge inputs (the literal empty string is expected; the UI skips empty anyway).

**Sleep / standby — model lifecycle (§2.8, 2026-07-19):** The most hardware-relevant subsystem, so Mari frees the machine when she's not needed. `infrastructure/model_manager.py` (`LmsModelManager` + `ModelManager` Protocol) drives the LM Studio `lms` CLI (`unload --all`, `load <model> --gpu max -y`) off the event loop; `available()` checks the CLI exists so bootstrap **auto-disables sleep** on a machine without it rather than flipping a state it can't honor. `SleepJob` fires after `SLEEP_AFTER_IDLE` (30 min) and calls `Companion.sleep()` — **flush pending first** (don't lose the buffer), set `_asleep`, then unload. While asleep the **model-using jobs pause** (reflection / reach-out / persona / idle-consolidation each early-return on `is_asleep()`), but **mood drift keeps ticking — the brain sleeps, the heartbeat doesn't** (§1.2). Wake is on the next message: `send()` reloads first (best-effort; a failed reload falls back to JIT / the LLM retry), and the web layer sends a `{type:"waking"}` frame so the cold-load delay shows as "waking up…" instead of looking hung. **Coverage:** `tests/test_tick.py` now 25 cases (sleep gated-by-idle / skipped-when-asleep / fires-after-long-idle, and model-jobs-skip-while-asleep). Self-waking (waking to reach out) is a deliberate **follow-up** — it needs a real trigger (a due reminder / energy budget / time-of-day) to be principled rather than twitchy; `wake()` is already a public seam for a future `WakeJob`. **Live-smoked** on qwen3.5-9b: warm chat → `sleep()` unloaded the model (VRAM freed, confirmed via `lms ps`) → a new message woke her, reloaded, and replied.

**Web UI overhaul — status panel + conversation tabs (2026-07-19):** Made the whole brain legible (the v1 "invisible bugs" pain) and gave the chat real conversation threads. **Status panel:** `GET /status` aggregates everything — awake/asleep, familiarity + label, the 6 mood channels, memory (core list + `count`/`count_core`/`count_superseded` + recent retired facts via new `memory_store.superseded()`), self-description, the thought journal, pending consolidation, and last reach-out/reflect/persona-edit times; a right-side panel polls it every 3s and renders mood bars, starred core facts, struck-through retired facts, etc. **Conversation tabs:** schema **v7** adds `sessions.title`; `conversation_store` gains `list_conversations()`, `session_messages()`, `latest_session()`, `set_title()`, `session_title()`, `delete_session()`; `Companion.switch_conversation()/new_conversation()` rebind only `history` + `session_id` (auto-titled from the first user message), while **memory, mood, thoughts, persona, familiarity, and the consolidation buffer/watermark stay global** — Mari is one companion across all threads. Boot now **resumes the most recent conversation** instead of spawning a session per launch. Web WS handles `switch`/`new`/`rename`/`delete`/`list` + a `conversations` frame + `history_start` (UI clears on switch); a left sidebar renders the tabs (click to switch, `+ New`, ×-to-delete, dbl-click rename). **Verified:** `GET /status` shape via FastAPI TestClient; a TestClient **WebSocket** run drove list → send (auto-title) → new → switch (history replay) → rename → delete. v7 migration non-destructive on the real DB (142 msgs / 12 memories; 11 prior launch-sessions surface as untitled conversations). One test fix: `test_durability`'s pre-v4 upgrade case now inserts its session via raw SQL (the new `create_session` writes `title`, which doesn't exist at v3).

**Tool framework — pillar 4 (2026-07-18):** The Tier-3 delivery layer, built robust-first per the user's ask ("make sure tool calling is robust so we can add more tools later"). **Probed before building:** `scripts/tool_probe.py` measured native OpenAI function-calling on qwen3.5-9b at **100% (18/18)** — right tool + valid args, no tool on plain chat, and a fed-back tool result used correctly — with `/no_think` on *and* off; a follow-up confirmed LM Studio **streams `tool_calls` as deltas** (`finish_reason=tool_calls`, empty content, arguments arriving in fragments). So the design streams the final answer normally and only loops when the model asks for a tool — no non-streaming fallback needed. **`core/tools.py`:** a `Tool` (name + description + JSON-schema params + async handler) and a hot-swappable **`ToolRegistry`** (`register`/`unregister`/`specs`/`__bool__`; `execute()` that *never raises* — unknown tool and handler exceptions become `[error: …]` result strings, non-dict args coerced, oversize results truncated). **`LLMClient.stream_with_tools`:** the streaming tool loop — accumulate `tool_call` deltas across chunks, on `finish_reason=tool_calls` run each via the registry and append `{role:"tool", …}` results, loop; the round that streams real content is the answer. Robust at **every seam**: malformed-arg JSON → error result fed back (turn still completes), unknown tool / handler error → same, a **`max_iters` safety net** forces a final tool-less answer so a turn can't hang, retry-before-first-emit (reusing the transient-retry pattern), and `<think>` stripping preserved. **Built-in tools (`core/builtin_tools.py`):** `get_current_time` (Windows-safe strftime; live-reliable) and **`reminisce`** — deliberate episodic recall that keyword-searches the full message log (`conversation_store.search_messages`, word-tokenized + ranked) plus the private journal for "remember when…" moments. **Integration:** `Companion` holds the registry and routes `send()` through `stream_with_tools` when non-empty (zero overhead otherwise); `TurnResult.tools` + the `done` WS frame surface invocations in the **response inspector** ("Tools called"). A `build_tools_note` block in `build_system` **reconciles tools with the persona** — the "you just met / don't invent history" rules were making the model disclaim memory instead of reminiscing, so the note clarifies that past-conversation recall is *real* history, allowed. **Coverage:** `tests/test_tools.py` (**20 offline cases**: registry contract + the loop driven by a fake streaming OpenAI client — call→execute→feed-back→answer, arg-fragment reassembly, malformed/unknown/handler-error, max-iters, retry, think-strip, plus reminisce retrieval + `search_messages`). **Live-verified** on qwen3.5-9b via `scripts/tool_smoke.py` + `scripts/reminisce_smoke.py`: `get_current_time` fires and is woven in naturally; plain chat and in-window facts correctly use **no** tool (no over-triggering); reminisce fires on episodic phrasings ("remember when I told you about my dog?") and recovers a fact scrolled out of the context window end-to-end (call → log search → answer). **Key finding:** reminisce is for *conversational episodes*, not *facts about the user* (name/job/pet) — those are core/semantic-memory's job via autonomic recall, and the model correctly routes between them. **The framework is ready for more tenants** (web search, Navidrome playlist creator, reminders, `rewrite_self`).

**Next: more tools on the now-proven framework — candidates: web search, a Navidrome/Subsonic playlist creator (mood-aware), a reminder tool, and exposing `rewrite_self` (§2.1) as a real tool. Self-initiated tool use in the tick is a follow-on. Backlog §2.9 has the user-liked GitHub-research features (drives/energy/nightly-consolidation).**

**Multi-drive proactivity — arc A1, "observe first" slice (2026-07-18):** Started the user-picked "autonomous inner life" arc (§2.9 GitHub research) that replaces the single idle-timer with something that feels alive. This first slice builds the **drive framework** and only *observes* it — the behaviors stay on their existing idle gates until the drives are shown to move sensibly against real use (the user's explicit choice: "framework only, observe first"). `core/drives.py` `DriveManager` is a Tier-1 autonomic sibling of `EmotionManager`: slow-integrating scalars in [0,1] that **rise while the user is away and relax while present**, **persisted** to the MetaStore like mood so they survive restarts. Two drives this slice: **connection** (the urge to reach out — rise sped by warmth/melancholy in the current mood, slowed by irritation; reset by contact, would be discharged by a reach-out) and **restlessness** (mental idleness that motivates reflection — rises faster, largely mood-independent, nudged up when interest is low; partly relieved by contact). Key design choice: drives **integrate by elapsed wall-time** (`dt` via an injected clock), so the dynamics are **independent of `TICK_INTERVAL`** (unlike mood's per-tick decay) and fully deterministic under a fake clock. Default rise rates are calibrated so — under neutral mood — each drive crosses its notional threshold near the current idle gate for the behavior it will eventually drive (restlessness ≈ reflection's 2-min gate, connection ≈ reach-out's 15-min gate), so the eventual rewire is a small tuning step. A **`DriveDriftJob`** (runs every tick, even while asleep — cheap state, no model call, and letting connection build during sleep is the future basis for a principled self-wake) updates them from `idle_seconds` + current mood; `Companion.on_user_message()` relieves them on each user turn. Surfaced **read-only** in `GET /status` + a "Drives" status-panel section (bars, like mood). `discharge()` is already exposed so wiring a behavior onto a drive later is a one-line gate change per job. **Coverage:** `tests/test_drives.py` (11 offline cases: rise-while-away, restlessness-faster, relax-while-present, warmth-speeds / irritation-slows connection, boredom-speeds restlessness, contact/discharge relief, clamp, zero-dt-noop, persistence) + `test_tick.py` now **28 cases** (added drive-drift idle/mood pass-through + no-op-without-drives). Full offline suite: **88 discrete PASS cases** (11 drives + 28 tick + 20 tools + 8 edge + 7 emotion + 6 retry + 4 durability + 4 core) plus the lifecycle script. **Live-verified end-to-end** via a TestClient boot of the real web app: `/status` shows the drives at baseline; a forced 15-min "away" integration drove connection to 0.84 (**above the ~0.6 neutral estimate because the real persisted mood's warmth/melancholy sped it up — mood modulation confirmed live**) and restlessness to saturation; contact fully relieved connection and halved restlessness; the new state persisted to the real MetaStore. Config: `DRIVES_ENABLED` (default on), `DRIVE_AWAY_AFTER` (90s). **Next in the arc:** watch the panel over real use to tune rise rates, then flip reach-out onto `connection` and reflection onto `restlessness` (option 1 of the scoping choice); then **energy/body cycles** (A2 — a third drive that makes sleep fire on internal state, not a 30-min timer) and **nightly consolidation** (A3).

**Memory inspector + admin controls (2026-07-18):** The §2.9 "memory inspector" backlog item, pulled forward because it's needed for practical testing (view/edit/delete memories + clear the DB). Chose the **web UI** surface (where testing happens) with **two destructive levels**, both confirm-gated. New **store ops** (kept behind the Protocols, updated in `core/interfaces.py`): `MemoryStore.all()` (every memory, active + retired, with id/core/active/superseded_by), `update_content()`, `delete()` (hard, vs. the lifecycle's soft `deactivate`), `clear()`; plus `clear()` on the conversation/thought/meta stores. **`MemoryManager.edit_memory(id, text)`** re-embeds the new wording so recall keeps matching. **`Companion.clear_memories()`** wipes just the semantic store; **`Companion.factory_reset()`** wipes *everything* — memories, messages+sessions, thoughts, and all meta (mood/drives/persona/watermark/cooldowns) — and resets the **in-memory** state too (`EmotionManager.reset()`/`DriveManager.reset()` re-persist baselines, history/buffer cleared, a fresh session created) so no reload is needed. **HTTP routes** (`web/app.py`): `GET /memories`, `POST /memory/{edit,delete,core,clear}`, `POST /admin/factory_reset` (rejects without an explicit `confirm` flag); mutations take the generation lock so they can't race a turn/consolidation. **Frontend:** a header "memory" button opens a modal listing all memories with an editable text field + core star + delete per row (retired ones struck through), and clear / full-reset in the footer (each `confirm()`s in-browser; reset does a `location.reload()`). **Coverage:** `tests/test_memory_admin.py` (7 offline cases: all/delete/clear/update_content, manager re-embed, clear-keeps-conversations, factory-reset-wipes-everything-and-baselines) — offline suite now **100 discrete PASS**. **Live-verified** end-to-end via a TestClient boot against a **throwaway DB** (never the real `companion.db`): every route drove correctly, edit re-embedded through LM Studio, factory-reset was confirm-gated and landed in a fresh empty session, and `GET /` serves the inspector markup.

**Multi-drive proactivity — behaviors wired onto the drives (2026-07-18):** After the observe-first demo confirmed the drives move sensibly, flipped **reach-out onto `connection`** and **reflection onto `restlessness`** (option 1 of the scoping choice). `ReachOutJob`/`ReflectionJob` gained a `drives` + `threshold` pair: `_wants_to()` gates on `drives.get(name) >= threshold` when drives are on, else the **old idle gate** (graceful fallback when `DRIVES_ENABLED=false`). On fire, the job **discharges** its drive (connection fully via `discharge("connection")`, restlessness partially) *and* marks the persisted cooldown **before** generating — so a decline/slow generation can't double-fire, and the cooldown stays a **hard floor** over the drive. Thresholds are config (`DRIVE_CONNECTION_THRESHOLD` 0.6, `DRIVE_RESTLESSNESS_THRESHOLD` 0.4), wired through bootstrap (reflection) + `web/app.py` (reach-out). **Tuning from the demo:** the observe-first run showed `restlessness` pegging at 1.0 within ~4 min (a step function), so its `RISE_PER_HOUR` was cut **15→5** (now ~0.4 at 5 min, saturates ~12 min — a usable gradient); `connection` (2.4/hr) was already well-graded (0.66 neutral vs 0.83 warm/sad at 15 min). Sleep stays on its idle timer (energy/body-cycles, A2, is the principled replacement). **Coverage:** `test_tick.py` now **33 cases** — added drive-gated reach-out/reflection (below-threshold no-fire, above-threshold fires + discharges, cooldown-is-a-hard-floor-over-the-drive); the pre-existing idle-gate cases stay green as the **drives-off fallback** coverage. Offline suite: **93 discrete PASS** (11 drives + 33 tick + 20 tools + 8 edge + 7 emotion + 6 retry + 4 durability + 4 core) plus the lifecycle script. Promoted the demo to **`scripts/drive_demo.py`** (a live observation harness that drives a conversation, replays the tick's mood/drive drift over away-gaps, and fires reflection/reach-out for real when a drive crosses threshold). **Live-verified end-to-end** on qwen3.5-9b: after a warm/sad chat, restlessness crossed 0.4 at 6 min → a melancholy-tagged journal entry about Alex; at 18 min connection hit 0.90 → an unprompted, appropriately gentle reach-out ("hope you're resting well. no pressure to reply."); a neutral chat would have reached the same point later. **The mood now sets the *timing* of proactivity — a fixed idle timer couldn't.** **Next in the arc:** energy/body cycles (A2) then nightly consolidation (A3); practical threshold/rate tuning is a user-testing task.

**Phase 1 — Core brain (the "solid base").** Chat with the emergent persona prompt (fixed core + empty self-description slot; anti-hallucination + message-shape rules). Fast memory save + recall with a calibrated embedder (measure thresholds live). Backgrounded/batched extraction. **Full conversation logging from day one** (episodic tier, §2.7). Basic tick loop running internally (reflection + mood drift, no outward messages yet). **Model-lifecycle manager** (§2.8) minimally in place so roles fit VRAM.

**Phase 2 — Memory lifecycle (pillar 1).** Contradiction-aware new/duplicate/update/supersede on extraction; soft-delete history. Deterministic tests on the fake store + live eval.

**Phase 3 — Emotion persistence + eval (pillar 2).** Persist the 6-channel mood to DB; run the behavioral eval; tune `PULL_STRENGTH`/decay/mapping against real transcripts. Wire mood drift into the tick.

**Phase 4 — Proactivity + sleep (pillar 3).** Tick decides + pushes real proactive messages over WebSocket, with throttling/appropriateness gate keyed off mood/presence. Full **sleep/standby** (§2.8): user- and bot-initiated model unload, "waking up…" state on reload. Definition of done: a real, unprompted, model-generated message arrives in the UI, and the bot can put itself to sleep.

**Phase 5 — Tool framework (pillar 4).** Registry + turn-loop integration; verify local tool-calling live; ship web search, **Navidrome playlist creator**, and **reminisce** (reads the episodic log); expose `rewrite_self` so the emergent persona (§2.1) can evolve; enable self-initiated tool use in the tick.

**Later — Voice, and pull from the §2.9 backlog** (familiarity meter, status panel, presence signal are the cheap early wins). STT/TTS slot in behind the existing pipeline.

---

## 4. Open questions still to decide

**Resolved during planning:** persona (emergent/self-modifying, §2.1), emotion mechanism (keep RoBERTa on CPU, §2.3), interface, LLM serving, build order, **runtime/concurrency** (single process + priority queue, §1.2), **capability model** (three tiers, §1.1), **v2 finish line** (bare-bones slice + rigorous bake-off, then reassess).

**Answered by testing, not by decision (Phase 0 bake-off):**
- **Chat/brain/embedding model choices** — including whether chat and brain can be the *same* model to save VRAM (v1 used llama-3.1-8b for both).
- **Embedding model** — commit to an asymmetric-prefix model (nomic/e5/bge/Qwen3-embedding; v1 used `text-embedding-qwen3-embedding-0.6b`) to fix perspective mismatch; confirm in the bake-off.
- **How far Tier-3 tools can go** — set by the measured tool-calling score.
- **VRAM residency policy** — which models may be resident together; what loads/unloads per role.

**Still open (decide after the basics work):**
- **Familiarity → persona coupling:** what the familiarity meter gates and how fast it rises.
- **Proactivity cadence:** default tick interval and reach-out frequency so it feels alive but not naggy.
- ~~**Tool-call turn loop shape:** streaming + tool-call interaction pattern, before Phase 5.~~ **Resolved (2026-07-18):** native streaming `tool_calls` loop — stream the answer, loop only when the model requests a tool; see the pillar-4 build-log entry.
- **Backlog triage:** which §2.9 items are in-scope vs. parked.
